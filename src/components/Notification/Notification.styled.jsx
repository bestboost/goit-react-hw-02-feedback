import styled from 'styled-components';

export const NotificBox = styled.div`

`;

export const NotificText = styled.p`
    font-size: ${p => p.theme.fontSizes.l}px;
    color: ${p => p.theme.colors.title};
    `;

